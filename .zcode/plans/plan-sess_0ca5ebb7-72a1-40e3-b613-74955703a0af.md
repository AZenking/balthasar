# 小屏 UI 线稿对齐改造计划

## 概述
保留功能、对齐线稿视觉。5 页 + 底栏,共 ~14 个文件改动。零 DB 迁移、零 API 变更。

## 改动清单(按文件)

### 1. 底栏 + FAB (2 files)
**`src/components/bottom-navigation.tsx`**
- 移除 active 顶部色条(`<span>` accent bar)
- 改用前景色 + 字重 + 图标色表达选中(`text-primary font-medium` 已有,加 `font-semibold`)
- 4 入口结构不变

**`src/components/transaction/transaction-drawer.tsx`**
- FAB `h-14 w-14`(56px) → `h-12 w-12`(48px)
- icon `h-6 w-6` → `h-5 w-5`

### 2. 首页 (4 files)
**`src/components/dashboard/dashboard-top-nav.tsx`**
- 账本名称后补 `›` 箭头(ChevronRight 图标),保持两行头部

**`src/components/dashboard/category-top-list.tsx`**
- 小屏(默认)显示前 3 项(非 4),第 4 项数据保留但不渲染
- section-head 加"全部"入口(`<button>` → `/transactions?type=expense`)
- 进度条 + 名称 + 金额结构不变

**`src/components/dashboard/expense-trend-chart.tsx`**
- TrendSection 标题从"本月支出趋势"改为"支出趋势"
- 右侧加环比徽标(传入 `comparisonPercent: number | null` prop)
- 徽标显示 `较上月 ↑X%` / `较上月 ↓X%` / 不显示(null)

**`src/app/(app)/dashboard/page.tsx`**
- DashboardBody 查询上月 summary 获取 comparisonPercent,传给 TrendSection
- 最近账单:小屏只渲染前 3 条(请 5 条,CSS `hidden` 或 slice)

**`src/components/dashboard/recent-transactions.tsx`**
- 主标题改为 remark 优先(空回退 categoryName)
- 副标题改为 `categoryName · accountName · time`
- 加 `maxItems` prop(默认 5,首页传 3)

### 3. 明细页 (3 files)
**`src/components/transactions/transaction-summary.tsx`**
- 改为三列卡片式:收入 / 支出 / 结余
- 移除 `bg-[var(--surface-secondary)]` 背景条
- 大金额 + 列分隔(`divide-x` 或 gap)

**`src/components/transactions/transaction-filters.tsx`**
- Tabs 始终显示(已有)
- "更多筛选"改为"筛选"按钮(打开高级筛选面板)
- 高级筛选改为 Modal/Drawer 而非内联展开(线稿式)
- 保留账户/分类选择 + 已选状态提示

**`src/components/transactions/transaction-list-item.tsx`**
- 主标题改为 remark 优先(空回退 categoryName)
- 副标题改为 `categoryName · accountName · time`
- 与 recent-transactions 统一主副标题逻辑

**`src/app/(app)/transactions/page.tsx`**
- 移除重复的"更多筛选"入口(仅保留工具栏筛选按钮)

### 4. 统计页 (3 files)
**`src/app/(app)/reports/page.tsx`**
- 第一行:"统计" + 月/年 toggle(已有)
- 第二行:周期切换 + 隐私按钮(已有,确认位置)
- 摘要卡补第三列"较上期"(用 `transaction.list(limit:1, includeSummary:true)` 查上一周期对比)
- 上一周期为 0 → "暂无对比"
- 趋势标题右侧加峰值徽标(从 trendData buckets 计算最大值)

**`src/components/reports/category-donut.tsx`**
- 加 `compact` prop(移动端缩小到 ~128-140px)
- 加 `centerLabel` prop(默认"本月支出",年模式"全年支出")
- 加 `centerSubLabel` prop(分类数量 + "支出")

**`src/components/reports/category-breakdown-card.tsx`**
- 合并到分类卡内(标题"分类占比" + "查看全部")
- "查看全部"展开至最多 5 项(默认 3 项)
- 卡内紧凑环图 + 前三分类

### 5. 设置页 (1 file)
**`src/app/(app)/settings/page.tsx`**
- 标题右侧加状态徽标("已同步"/"本地使用")
- 偏好项顺序调整:隐私保护 → 记账提醒 → 默认账户 → 外观主题
- 外观主题显示文本值("跟随系统"/"浅色"/"深色")
- API Key 收纳到"高级设置"分组(默认折叠)
- "清空全部数据"移到独立危险操作区

### 6. 共享组件 (1 new file)
**`src/components/shared/comparison-badge.tsx`** (新建)
- 输入:`{ current: number, previous: number | null, label?: string }`
- 输出:`↑X%` / `↓X%` / "暂无对比"(previous=0)
- 颜色:上升红(支出增加)、下降绿(支出减少)

## 执行顺序
1. 共享组件 comparison-badge(无依赖)
2. 底栏 + FAB(全局影响)
3. 首页 4 文件
4. 明细 3 文件
5. 统计 3 文件
6. 设置 1 文件
7. type-check + build + commit

## 验证
- `pnpm type-check`
- `pnpm build`(12 页全生成)
- `pnpm test:unit`(确保现有测试不回归)